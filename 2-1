#include <bits/stdc++.h>
using namespace std;


int main(void){
  ios::sync_with_stdio(0);
  cin.tie(0);
  int A[10000];
  int X,N;
  cin>>N>>X;
  for(int i=0;i<N;i++){
    cin>>A[i];
    if(A[i]<X){
      cout<<A[i]<<' ';
    }
  }

}
