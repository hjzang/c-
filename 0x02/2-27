#include <bits/stdc++.h>
using namespace std;
int main(void){
  ios::sync_with_stdio(0);
  cin.tie(0);
  int a[9],max=0,cnt=0;
  for(int i=0;i<9;i++){
    cin>>a[i];
    if(a[i]>max){
      max=a[i];
      cnt=i+1;
    }

  }
  cout<<max<<'\n'<<cnt;
}
